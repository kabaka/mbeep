version: 2
updates:
  # Keep the GitHub Actions used in CI/CodeQL up to date.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
